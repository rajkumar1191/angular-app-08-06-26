import { ProjectedItemDirective } from './projected-item-directive';

describe('ProjectedItemDirective', () => {
  it('should create an instance', () => {
    const directive = new ProjectedItemDirective();
    expect(directive).toBeTruthy();
  });
});
